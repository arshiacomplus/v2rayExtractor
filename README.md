### 🤖 **فعال‌سازی ارسال نتایج به تلگرام**

این پروژه قابلیت ارسال خودکار نتایج (خلاصه و لیست کانفیگ‌های سالم) به کانال‌های تلگرام شما را دارد. برای فعال‌سازی این قابلیت، مراحل زیر را دنبال کنید:

---

#### **پیش‌نیازها:**

قبل از شروع، شما به سه مورد زیر نیاز دارید:

1.  **توکن ربات تلگرام:** که می‌توانید آن را از [BotFather](https://t.me/BotFather) دریافت کنید.
2.  **آیدی کانال اصلی:** برای ارسال پیام خلاصه نتایج (مثلاً `@arshia_mod_fun`).
3.  **آیدی کانال کانفیگ‌ها:** برای ارسال کانفیگ‌های گروه‌بندی شده (مثلاً `@v2ray_Extractor`).

**نکته مهم:** ربات شما باید در هر دو کانال **ادمین** باشد و دسترسی لازم برای ارسال پیام را داشته باشد.

---

#### **مرحله ۱: افزودن Secrets به ریپازیتوری**

برای امنیت، توکن و آیدی‌ها باید به عنوان `Secrets` در ریپازیتوری گیت‌هاب شما ذخیره شوند.

1.  به ریپازیتوری خود در گیت‌هاب بروید و مسیر زیر را دنبال کنید:
    `Settings` > `Secrets and variables` > `Actions`

2.  روی دکمه `New repository secret` کلیک کنید و سه `secret` جدید با نام‌های **دقیق** زیر بسازید و مقادیر مربوطه را در آن‌ها قرار دهید:

    *   `TELEGRAM_BOT_TOKEN`
        *   **مقدار:** توکن ربات خود را که از BotFather گرفته‌اید، اینجا وارد کنید.
    *   `TELEGRAM_CHAT_ID`
        *   **مقدار:** آیدی کانال اصلی خود را (با `@`) اینجا وارد کنید.
    *   `TELEGRAM_CHANNEL_ID`
        *   **مقدار:** آیدی کانال دوم (کانال کانفیگ‌ها) را (با `@`) اینجا وارد کنید.

---

#### **مرحله ۲: فعال‌سازی در فایل Workflow**

حالا باید به GitHub Actions بگویید که قابلیت ارسال به تلگرام را فعال کند.

1.  فایل `.github/workflows/main.yml` را در ریپازیتوری خود باز کنید.
2.  در بخش `Run Main Extractor Script`، مقدار `SEND_TO_TELEGRAM` را به `true` تغییر دهید.

    ```yaml
    - name: Run Main Extractor Script
      env:
        # ----------------------------------------------------
        # برای فعال‌سازی، این مقدار را true قرار دهید
        SEND_TO_TELEGRAM: true 
        # ----------------------------------------------------
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        TELEGRAM_CHANNEL_ID: ${{ secrets.TELEGRAM_CHANNEL_ID }}
      run: python main.py
    ```

با انجام این دو مرحله، پس از هر بار اجرای موفق workflow، نتایج به صورت خودکار به کانال‌های تلگرام شما نیز ارسال خواهد شد.

---


### 🌟 **ساب لینک‌های کانفیگ کالکتور** 🌟

برای استفاده، لینک‌های زیر رو در کلاینت وارد کن:
---

### 🔹 **mix**:
```bash
https://raw.githubusercontent.com/arshiacomplus/v2rayExtractor/refs/heads/main/mix/sub.html
```

---

---

### 🔹 **vless**:
```bash
https://raw.githubusercontent.com/arshiacomplus/v2rayExtractor/refs/heads/main/vless.html
```

---

### 🔹 **vmess**:
```bash
https://raw.githubusercontent.com/arshiacomplus/v2rayExtractor/refs/heads/main/vmess.html
```

---

### 🔹 **trojan**:
```bash
https://raw.githubusercontent.com/arshiacomplus/v2rayExtractor/refs/heads/main/trojan.html
```

---

### 🔹 **ss**:
```bash
https://raw.githubusercontent.com/arshiacomplus/v2rayExtractor/refs/heads/main/ss.html
```

---

### 🔹 **hy2**:
```bash
https://raw.githubusercontent.com/arshiacomplus/v2rayExtractor/refs/heads/main/hy2.html
```



## 🌍 **در این پیج می‌تونی کانفیگ‌ها رو بر اساس منطقه مشاهده کنی:**


https://arshiacomplus.github.io/V2rayExtractor-page/

---
